{
  "name": "airdlivers-bot",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "dev": "nodemon bot.js",
    "build": "echo 'Starting Build...' && cd airdlivers-web && npm install --no-audit --no-fund --legacy-peer-deps && CI=false GENERATE_SOURCEMAP=false npm run build && echo 'Build Complete.'"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "form-data": "^4.0.5",
    "fs-extra": "^10.1.0",
    "jsonwebtoken": "^9.0.3",
    "moment": "^2.29.4",
    "mongodb": "^6.0.0",
    "multer": "^2.0.2",
    "node-fetch": "^3.3.2",
    "node-telegram-bot-api": "^0.61.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
